# RF02 - Configurar a conexão com o PostgreSQL utilizando TypeORM

A aplicação deverá estabelecer conexão com um banco de dados PostgreSQL utilizando o TypeORM (DataSource). As informações de conexão deverão ser armazenadas em um arquivo .env, evitando que credenciais fiquem diretamente no código-fonte.
